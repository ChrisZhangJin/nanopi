---
phase: quick-260902-m0z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/hook.rs
  - src/agent/loop_.rs
  - src/agent/permission.rs
  - src/config.rs
  - src/settings.rs
  - src/settings_toml.rs
  - src/mode/tui.rs
  - src/mode/print.rs
  - config.toml.example
  - README.md
  - README_zh.md
  - docs/v0.5-research.md
  - .release-notes-v0.12.0.md
autonomous: true
requirements: [STAGE-A]

must_haves:
  truths:
    - "A hook configured under `[[hooks.tool_execution_start]]` fires before a tool call; the same config under `[[hooks.pre_tool_use]]` is a hard config-load error naming the new key."
    - "The four retired keys are gone outright — no alias, no dual-key parsing, no warn-and-accept path (D-01)."
    - "`NANOPI_EVENT` is `ToolExecutionStart` / `ToolExecutionEnd` / `Input` / `SessionShutdown` for the four renamed events."
    - "A `session_shutdown` hook fires on `/new`, `/resume`, `/fork`, `/import`, and on quit, and its stdin payload carries `arguments.reason` = `new`/`resume`/`fork`/`import`/`quit`."
    - "The `matcher` subject for `session_shutdown` is still the session id."
    - "`cargo test --features wasm` is green at every commit."
    - "Both READMEs still claim the Claude Code hook *protocol* and no longer claim Claude Code event *names*, and carry the old→new rename table."
    - "`.release-notes-v0.12.0.md` exists and leads its Compatibility section with the rename table and the `session_shutdown` semantics change."
  artifacts:
    - path: src/agent/hook.rs
      provides: "Renamed HookEvent variants, env_var strings, run_session_hooks arguments param, retired-key error mapping"
      contains: "ToolExecutionStart"
    - path: src/agent/loop_.rs
      provides: "fire_session_shutdown(reason) plus renamed HooksConfig fields"
      contains: "fire_session_shutdown"
    - path: src/mode/tui.rs
      provides: "Four new session_shutdown emit sites at the agent-swap points"
    - path: .release-notes-v0.12.0.md
      provides: "The user-facing announcement of the break"
      contains: "tool_execution_start"
  key_links:
    - from: src/mode/tui.rs
      to: Agent::fire_session_shutdown
      via: "called on the OLD agent before `*g = Some(new_agent)`"
      pattern: "fire_session_shutdown\\("
    - from: src/config.rs
      to: src/agent/hook.rs
      via: "toml unknown-field error mapped through the retired-key table"
      pattern: "retired_hook_key_error|legacy_hook_key"
---

<objective>
Stage A of `docs/v0.12-events.md`: adopt PI's names for the four
Claude-Code-named hook events on both the `[[hooks.*]]` config surface and
the `NANOPI_EVENT` env var, make the retired keys a hard config error, and
give `session_shutdown` a `reason` fired at all five teardown sites.

Purpose: one vocabulary for nanopi's eleven events, before Stage B wires
WASM plugins to the same event set. Ships alone; no WASM work here.
Output: renamed code + config surface, five `session_shutdown` emit sites,
the retired-key error, updated READMEs / `config.toml.example` /
`docs/v0.5-research.md` banner, and `.release-notes-v0.12.0.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/v0.12-events.md
@.planning/STATE.md

@src/agent/hook.rs
@src/agent/loop_.rs
@src/settings.rs
@src/config.rs
@src/mode/tui.rs

@.release-notes-v0.11.0.md

**The spec is frozen.** §2.1 (rename map), §2.2 (`session_shutdown` +
`reason`), and the Stage A half of the §9 checklist are authoritative. Do
not redesign; do not extend into Stage B (no WASM, no `events` list, no
`list-events`).

**D-01 — no compatibility period.** The four retired keys stop working
outright. Do NOT add deprecation aliases, `#[serde(alias = ...)]`,
dual-key parsing, a migration shim, or an "old key still accepted with a
warning" path. Rationale: effectively no users of these keys yet, so a
compatibility layer costs more than it buys. Two consequences, both
first-class deliverables of Task 3:
- The retired-key error is the ONLY thing between an old config and silent
  breakage, so it must be a **hard error** naming the replacement — not a
  stderr line that scrolls past, and not a generic "unknown key".
- `.release-notes-v0.12.0.md` is how the break is communicated, so it is
  part of Stage A's definition of done, not a follow-up.

Branch is `v0.12.0`. Two untracked `.png` files at the repo root are not
yours — leave them alone. Do NOT `git checkout` / `git restore` any file,
and do NOT revert or amend `ead885a` or `36aed27`.

<interfaces>
The rename map (spec §2.1 — the only four events that change):

| old key / variant / env | new key / variant / env |
|---|---|
| `pre_tool_use` · `PreToolUse` · `PreToolUse` | `tool_execution_start` · `ToolExecutionStart` · `ToolExecutionStart` |
| `post_tool_use` · `PostToolUse` · `PostToolUse` | `tool_execution_end` · `ToolExecutionEnd` · `ToolExecutionEnd` |
| `user_prompt_submit` · `UserPromptSubmit` · `UserPromptSubmit` | `input` · `Input` · `Input` |
| `session_end` · `SessionEnd` · `SessionEnd` | `session_shutdown` · `SessionShutdown` · `SessionShutdown` |

`HookEvent` carries `#[serde(rename_all = "snake_case")]`, so renaming the
variants renames the JSON payload's `event` value. The `[[hooks.*]]` table
keys are `HooksSection`'s **field names** (`src/settings.rs:49`) and are
renamed separately from the enum.

Field-name sites (all `Vec<HookConfig>`), verified at HEAD:
- `src/settings.rs:51,53,55,59` — `HooksSection` declarations
- `src/settings.rs:79-83,103-109,131-135,150-154,170-175` — `From<Settings>`, three merge blocks, `validate_hooks` calls
- `src/config.rs:272-289` — `merge()`
- `src/agent/loop_.rs:55-59` — `HooksConfig` declarations
- `src/mode/tui.rs:3320-3324` — hook-count display

Session hook plumbing:
- `run_session_hooks(hooks, event, subject, cwd)` — `src/agent/hook.rs:396`, hardcodes `arguments: json!({})` at line 423; `debug_assert!` at 405 lists the accepted variants.
- Callers: `src/agent/loop_.rs:286` (session_start), `302` (session_end), `330` + `365` (compaction).
- `Agent::fire_session_end` — `src/agent/loop_.rs:298`.
- Callers of `fire_session_end`: `src/mode/tui.rs:505`, `src/mode/print.rs:188`.

Agent-swap sites in `src/mode/tui.rs` (the four NEW emits go immediately
before each `*g = Some(new_agent)`, on the OLD agent behind the same
guard):
- `1908`/`1909` — `KeyAction::NewSession` → reason `new`
- `2025`/`2026` — `KeyAction::OpenResumePicker` → reason `resume`
- `2475`/`2476` — `KeyAction::ApplyImport` → reason `import`
- `3161`/`3162` — `execute_fork` → reason `fork`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename the four events across code, doc comments, and tests</name>
  <files>src/agent/hook.rs, src/agent/loop_.rs, src/agent/permission.rs, src/config.rs, src/settings.rs, src/settings_toml.rs, src/mode/tui.rs</files>
  <behavior>
    - `HookEvent::ToolExecutionStart.env_var() == "ToolExecutionStart"`; same for `ToolExecutionEnd`, `Input`, `SessionShutdown`.
    - `serde_json::to_string(&HookEvent::Input) == "\"input\""` and round-trips; same for the other three snake_case forms.
    - A `HookInput { event: HookEvent::Input, .. }` serializes with `"event":"input"` (replaces the existing `"event":"user_prompt_submit"` assertion at `src/agent/hook.rs:867`).
    - `[[hooks.tool_execution_start]]` in config.toml loads into `HooksSection::tool_execution_start`; the config.rs merge test still concatenates two sources in order.
    - `PermissionMode::should_honor_tool_execution_start_block()` keeps its existing truth table.
  </behavior>
  <action>
Do this rename **by hand, per site — not with sed.** A blind sed was tried
and reverted; these are the traps it hit and they are the reason this task
exists as its own commit. Per D-01, this is a pure rename — no aliases and
no old-name fallbacks anywhere.

1. `src/agent/hook.rs`: rename the four `HookEvent` variants and their
   `env_var()` arms per the table in `<interfaces>`. Update the module doc
   comment (lines 1–20) so its `[[hooks.pre_tool_use]]` example and its
   snake_case key list use the new names. Update the `run_hooks` doc
   comment at 318–322 (`post_tool_use` / `pre_tool_use` prose) and the
   `run_session_hooks` doc comment at 386–394 plus its `debug_assert!`
   (`HookEvent::SessionEnd` → `SessionShutdown`).
2. `src/settings.rs`: rename the four `HooksSection` fields and every
   downstream use (`From<Settings>`, the three merge blocks, the
   `validate_hooks` calls) per the site list in `<interfaces>`. Rename the
   tests and their embedded TOML: `loads_pre_tool_use` →
   `loads_tool_execution_start`, and the `[[hooks.pre_tool_use]]` /
   `[[hooks.post_tool_use]]` fixture text at 253, 259, 292, 304, 391 plus
   the assertions at 240–241, 269–271, 328–330 and the comments at 288/300.
3. `src/config.rs`: rename the `merge()` field moves and extends
   (272–289), the fixture TOML at 493, the assertions at 508–509, and the
   merge test's `HooksSection` literals at 517–543.
4. `src/agent/loop_.rs`: rename the four `HooksConfig` fields (55–59) and
   every use. **`user_prompt_submit` → `input` is the dangerous one**: the
   file is full of the unrelated word `input` (`HookInput`, `tool_input`,
   `updated_input`, `let input = HookInput { .. }`, `input_json`). Only the
   `self.hooks.user_prompt_submit` / `hooks.user_prompt_submit` /
   `user_prompt_submit:` struct-literal occurrences (523, 525, 1974) and
   the `HookEvent::UserPromptSubmit` at 526 change. Also update the prose
   and user-visible strings: the comments at 460, 500, 516, 519, 567, 1408,
   1489–1491, 1522, 1756–1757, 1763, 1870, 1941, 2716, 2740; the blocked
   marker string at 539 (`[UserPromptSubmit hook blocked the prompt: ...]`
   → `[Input hook blocked the prompt: ...]`); the test names
   `post_tool_use_hook_receives_input_and_response` →
   `tool_execution_end_hook_receives_input_and_response`,
   `post_tool_use_hook_can_transform_result` →
   `tool_execution_end_hook_can_transform_result`,
   `before_agent_start_output_feeds_user_prompt_submit` →
   `..._feeds_input`; the payload assertion at 1740
   (`v["event"] == "post_tool_use"` → `"tool_execution_end"`); and the
   assertion messages at 2028 and 2781.
5. `src/agent/permission.rs`: `should_honor_pretooluse_block` →
   `should_honor_tool_execution_start_block` (60), its doc comment (57),
   the test name `pretooluse_block_always_honored` →
   `tool_execution_start_block_always_honored` (143) and its call (145).
   This identifier contains no `pre_tool_use` substring, which is exactly
   why the sed attempt missed it. Update the caller at
   `src/agent/loop_.rs:1423`.
6. `src/mode/tui.rs:3320-3324`: rename the four fields in the hook-count
   sum.
7. `src/settings_toml.rs:245,264`: the test fixture writes
   `[[hooks.pre_tool_use]]` and asserts it survives a `save()`. Rename both
   to `tool_execution_start` — the point of the test is comment/unknown-
   section preservation, and it must not depend on a retired key.

Leave `fire_session_end` and its two callers alone in this task except for
the `HookEvent::SessionShutdown` / `self.hooks.session_shutdown` bodies —
the function rename and `reason` are Task 2.

After editing, prove no stragglers remain (see `<verify>`); the counts in
the plan brief were taken at HEAD and a nonzero residue means a missed site.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; ! grep -rniE 'pre_tool_use|PreToolUse|post_tool_use|PostToolUse|user_prompt_submit|UserPromptSubmit|pretooluse' src/ &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>`cargo test --features wasm` green (565 unit + 6 + 22 integration, no
  net change); clippy clean; zero occurrences of the three old
  tool/prompt names anywhere under `src/`. Committed as one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: session_shutdown gains a reason, fired at all five teardown sites</name>
  <files>src/agent/hook.rs, src/agent/loop_.rs, src/mode/tui.rs, src/mode/print.rs</files>
  <behavior>
    - A `session_shutdown` hook receives stdin JSON whose `arguments` object is `{"reason":"quit"}` (or `new`/`resume`/`fork`/`import`), while `session_id` and the `matcher` subject remain the session id.
    - `run_session_hooks` still sends `arguments: {}` for `session_start` and the two compaction events (no payload change to shipped events).
    - A `session_shutdown` hook whose `matcher` is `^prod-` does not fire for session id `dev-1234` (the existing matcher test, unchanged in meaning).
    - `--no-hooks` still suppresses `session_shutdown` (extends the existing `fire_session_start`/`fire_session_end` gating test).
  </behavior>
  <action>
Per spec §2.2.

1. `src/agent/hook.rs`: add an `arguments: Value` parameter to
   `run_session_hooks` (after `event`, before `subject`) and use it in
   place of the hardcoded `json!({})` at line 423. Update the doc comment
   to say `arguments` is the event's payload object and that `subject` —
   and therefore `matcher` — is unchanged by this addition.
2. `src/agent/loop_.rs`: pass `json!({})` at the three unchanged call
   sites (286, 330, 365). Rename `fire_session_end` →
   `fire_session_shutdown(&self, reason: &str)`; it passes
   `json!({"reason": reason})` and `HookEvent::SessionShutdown`, keeps the
   `hooks_active()` gate, and keeps `&self.session_id.to_string()` as the
   subject. Update its doc comment to list the five reasons
   (`quit`/`new`/`resume`/`fork`/`import`) and to record the two deliberate
   divergences from PI: no `reload` (nanopi's `/reload` updates the live
   agent in place, so no session ends) and `import` added (nanopi has
   `/import`, PI does not).
3. `src/mode/print.rs:188`: `fire_session_shutdown("quit").await`.
4. `src/mode/tui.rs:505`: `fire_session_shutdown("quit").await`.
5. Four NEW emits in `src/mode/tui.rs`, at the agent-swap points listed in
   `<interfaces>`. Each must fire on the **old** agent **before**
   `*g = Some(new_agent)` — after the swap the hook would run against the
   wrong session, which is the whole reason these sites are enumerated.
   The guard `g` is already `let mut g = agent_slot.lock().await;` at each
   site, so the shape is: take `g.as_ref()`, `await`
   `fire_session_shutdown("<reason>")` on it, then assign. Reasons:
   `new` (1908), `resume` (2025), `import` (2475), `fork` (3161).
   Holding the mutex across the await is fine — it is the same task and
   the hook does not re-enter `agent_slot`.
6. Tests in `src/agent/loop_.rs`: extend the existing session-lifecycle
   test (`~2716`) to `fire_session_shutdown("quit")`, and add one test
   that wires a `session_shutdown` hook whose command writes its stdin to
   a temp file, fires `fire_session_shutdown("new")`, and asserts the
   captured JSON contains `"reason":"new"` and `"event":"session_shutdown"`
   and that `session_id` is the agent's session id (proving the matcher
   subject did not change). Follow the temp-file-marker pattern already
   used by `run_session_hooks_fires_matching_hook` in `hook.rs`.

The four TUI emits are not unit-testable (they need a live terminal), so
their proof is the grep gate in `<verify>` plus the compile: exactly five
`fire_session_shutdown(` call sites across `src/mode/`, with the reasons
spelled correctly.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; test "$(grep -rho 'fire_session_shutdown("[a-z]*")' src/mode/ | sort -u | tr '\n' ' ')" = 'fire_session_shutdown("fork") fire_session_shutdown("import") fire_session_shutdown("new") fire_session_shutdown("quit") fire_session_shutdown("resume") ' &amp;&amp; ! grep -rn 'fire_session_end\|HookEvent::SessionEnd\|session_end' src/ &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>All five reasons present exactly once each in `src/mode/`, each of the
  four new ones textually preceding its `*g = Some(new_agent)`; no
  `fire_session_end` / `session_end` left under `src/`; suite green;
  clippy clean. Committed as one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retired keys are a hard error; docs, config example, release notes</name>
  <files>src/settings.rs, src/config.rs, src/agent/hook.rs, config.toml.example, README.md, README_zh.md, docs/v0.5-research.md, .release-notes-v0.12.0.md</files>
  <behavior>
    - Loading a `config.toml` containing `[[hooks.pre_tool_use]]` returns `Err` (hard error — nanopi does not start), and the message names both the retired key and `tool_execution_start` and points at `docs/v0.12-events.md §2.1`.
    - Same for `post_tool_use` → `tool_execution_end`, `user_prompt_submit` → `input`, `session_end` → `session_shutdown`, in `settings.toml` as well as `config.toml`.
    - A hook key that is merely misspelled (e.g. `[[hooks.turn_startt]]`) is also a hard error, falling through to serde's unknown-field message listing the valid keys.
    - A `config.toml` using only current keys still loads clean (the existing loader tests).
    - `.release-notes-v0.12.0.md` exists, follows the house style of `.release-notes-v0.11.0.md`, and its `## Compatibility` section opens with the four-row rename table.
  </behavior>
  <action>
Spec §2.1's "the one config typo that must be loud", now load-bearing
because D-01 removed the compatibility path entirely.

1. Add `#[serde(deny_unknown_fields)]` alongside the existing
   `#[serde(default)]` on `HooksSection` (`src/settings.rs:47-49`). Both
   `config.toml` (via `Config`) and `settings.toml` (via `Settings`) reach
   the same struct, so one attribute covers both surfaces. Comment that
   this is what makes a retired key a parse error instead of a silent
   no-op, and that per D-01 there is deliberately no alias to soften it.
2. Add to `src/agent/hook.rs`:
   `pub fn retired_hook_key_error(err: &str) -> Option<String>` — a
   four-entry table (`pre_tool_use` → `tool_execution_start`,
   `post_tool_use` → `tool_execution_end`, `user_prompt_submit` → `input`,
   `session_end` → `session_shutdown`) scanned against a rendered toml
   error's text, returning a message of the shape:
   `unknown hook event "pre_tool_use" — renamed to "tool_execution_start" in v0.12 (see docs/v0.12-events.md §2.1)`
   The four old names must be recognised by name and mapped to their
   replacement rather than falling into the generic unknown-key path.
   Unit-test all four mappings plus a `None` case (an unrelated error
   string must not be rewritten).
3. Wire it into both error paths so the mapped message reaches the user in
   place of serde's bare "unknown field": `src/config.rs::load_one` (258)
   and `src/settings.rs::load_one`. When `retired_hook_key_error` yields a
   message, surface that (keeping the file path so the user knows which of
   the four config files to edit); otherwise keep today's error verbatim.
   Add a loader test in each file that writes a `[[hooks.pre_tool_use]]`
   file and asserts the returned `Err`'s string contains both
   `pre_tool_use` and `tool_execution_start`. Add one more asserting a
   misspelled-but-never-shipped key (`turn_startt`) is still an `Err`.
4. `config.toml.example` (195–245): rename the four commented keys, and
   add a short "renamed in v0.12 — the old keys are errors, not warnings"
   comment block with the four-row old→new table above the hooks section.
5. `README.md` — line 45 (feature bullet), 195 (section intro), 198 (TOML
   sample), 203 (snake_case note), 207 (lifecycle paragraph). Keep the
   Claude Code **protocol** claim (JSON on stdin, exit 2 to refuse,
   `tool_name` / `tool_input` / `hookSpecificOutput` fields) and drop the
   Claude Code **naming** claim: the event names are PI's now. Add the
   four-row rename table so a reader arriving from Claude Code finds the
   new key, and say plainly that the old keys are rejected at startup.
   Leave line 122 (`-p` mode semantics) and 347 (credits) alone — those
   claims are still true. Mirror all of it in `README_zh.md`
   (44, 190, 193, 198, 202); per the repo's bilingual convention the
   Chinese version is a real translation, not a stub.
6. `docs/v0.5-research.md` §6 (line 647): add a one-line banner
   immediately under the heading pointing at `docs/v0.12-events.md` §2.1
   for the current event names. **Do not rewrite the section** — it is a
   historical record of the v0.5 design.
7. Create `.release-notes-v0.12.0.md`, matching the house style of
   `.release-notes-v0.11.0.md` (read it first: `# nanopi vX.Y.Z`, a
   two-or-three-sentence lede, `## Highlights` with `###` subsections,
   then `## Compatibility`, then `## Under the hood`). Content:
   - Lede: nanopi's eleven lifecycle events now have one vocabulary, PI's.
   - `## Compatibility` **leads with the breaking change**, unlike v0.11's
     "no breaking changes": the four-row rename table first, then the
     statement that the old keys are a startup error naming the
     replacement (no deprecation period — D-01, and say why: effectively
     no users of these keys yet), then the `session_shutdown` semantics
     change — it now fires on `/new`, `/resume`, `/fork`, `/import` as
     well as quit, and carries `arguments.reason`; a hook that assumed
     "fires once, at exit" needs to branch on `reason`. Note the
     `matcher` subject is unchanged (still the session id).
   - `## Under the hood`: the `NANOPI_EVENT` values follow the rename;
     `HooksSection` gained `deny_unknown_fields`.
   - Do not describe Stage B (WASM event delivery) — it has not shipped.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; grep -q 'deny_unknown_fields' src/settings.rs &amp;&amp; grep -c 'tool_execution_start' README.md README_zh.md config.toml.example .release-notes-v0.12.0.md &amp;&amp; grep -A2 '^## 6' docs/v0.5-research.md | grep -q 'v0.12-events' &amp;&amp; grep -q '^## Compatibility' .release-notes-v0.12.0.md &amp;&amp; ! grep -rn 'serde(alias' src/settings.rs src/config.rs &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>A `[[hooks.pre_tool_use]]` config fails to load with an error naming
  both the retired key and `tool_execution_start`, proven by a test in each
  of `config.rs` and `settings.rs`; no `serde(alias …)` anywhere; both
  READMEs, `config.toml.example`, and `.release-notes-v0.12.0.md` carry the
  rename table; `docs/v0.5-research.md` §6 has the banner and is otherwise
  unchanged (`git diff --stat` shows a 1–2 line delta for that file); suite
  green; clippy clean. Committed as one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.toml` / `settings.toml` → hook runtime | user-authored TOML selects shell commands nanopi will execute |
| hook subprocess stdin/stdout → agent loop | hook output can block or rewrite tool arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m0z-01 | Tampering | retired `[[hooks.pre_tool_use]]` key | mitigate | Task 3: `deny_unknown_fields` + the four-entry retired-key table, surfaced as a hard load error. With D-01 removing the alias path, a blocking guard (`check-rm-rf.sh`) silently un-registering after upgrade is the failure mode this error exists to prevent — which is why it must abort startup, not warn. |
| T-m0z-02 | Repudiation | `session_shutdown` at agent-swap sites | mitigate | Task 2: fire on the OLD agent before `*g = Some(new_agent)`, so an audit/cleanup hook records the session that actually ended, not its successor. |
| T-m0z-03 | Information disclosure | `reason` in the payload | accept | The five reasons are fixed literals chosen by nanopi (`quit`/`new`/`resume`/`fork`/`import`); no user data, no path, no prompt text crosses the boundary. |
| T-m0z-04 | Elevation of privilege | `matcher` semantics | accept | Spec §2.2 explicitly keeps the subject as the session id. Switching it to the reason would silently widen every existing matcher's effective scope — deliberately not done in v0.12. |
| T-m0z-05 | Denial of service | `session_shutdown` now fires on `/new` `/resume` `/fork` `/import` | accept | A slow hook now delays four interactive commands it previously did not touch, bounded by each hook's own `timeout` (default 5000 ms) — the same bound already accepted at quit. Called out in the release notes' Compatibility section so it is a known cost. |
| T-m0z-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan (`Cargo.toml` is untouched). If a task turns out to need one, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
1. `cargo test --features wasm` green after each of the three commits —
   565 unit + 6 + 22 integration at HEAD, and this plan adds tests without
   removing any, so the counts only go up.
2. `cargo clippy --features wasm --all-targets -- -D warnings` clean.
3. `grep -rniE 'pre_tool_use|PreToolUse|post_tool_use|PostToolUse|user_prompt_submit|UserPromptSubmit|pretooluse|session_end|SessionEnd' src/`
   → only the retired-key table in `src/agent/hook.rs` and its tests, and
   the loader tests that assert the error. Nothing in a live code path.
   (`docs/v0.5-research.md`, `docs/PLAN.md`, `docs/pi-vs-nanopi.md`, and
   `.planning/` keep the old names on purpose — historical records. Only
   `src/`, `config.toml.example`, the two READMEs, and the release notes
   are in scope.)
4. `git status --porcelain` shows only the two untracked root `.png` files
   beyond the committed changes.
5. Manual smoke, one command per surface:
   `printf '[[hooks.session_shutdown]]\nmatcher="*"\ncommand="cat >/tmp/npshut.json"\n' > .nanopi/config.toml`,
   run `nanopi -p hi`, then confirm `/tmp/npshut.json` contains
   `"event":"session_shutdown"` and `"reason":"quit"`.
   Then swap the key to `[[hooks.pre_tool_use]]` and confirm nanopi
   **refuses to start**, printing a message that names
   `tool_execution_start`.
</verification>

<success_criteria>
- Every Stage A box in `docs/v0.12-events.md` §9 is satisfied: enum +
  `env_var()`, `HooksSection` keys in all three files, hard unknown-key
  error, `fire_session_shutdown(reason)` at all five sites, `permission.rs`
  rename, README ×2 + `config.toml.example` + rename table,
  `docs/v0.5-research.md` §6 banner.
- D-01 honored: zero aliases, zero dual-key parsing, zero warn-and-accept
  paths for the four retired keys.
- `.release-notes-v0.12.0.md` created, Compatibility section leading with
  the rename table and the `session_shutdown` semantics change.
- Three commits, suite green at each.
- No Stage B work leaked in (no `.wit` change, no `events` config field,
  no `list-events` / `handle-event`).
</success_criteria>

<output>
Create `.planning/quick/260902-m0z-rename-hook-events-pi-names/260902-m0z-SUMMARY.md` when done
</output>
