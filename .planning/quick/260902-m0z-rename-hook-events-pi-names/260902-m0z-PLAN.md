---
phase: quick-260902-m0z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/hook.rs
  - src/agent/loop_.rs
  - src/agent/permission.rs
  - src/config.rs
  - src/settings.rs
  - src/settings_toml.rs
  - src/mode/tui.rs
  - src/mode/print.rs
  - config.toml.example
  - README.md
  - README_zh.md
  - docs/v0.5-research.md
  - .release-notes-v0.12.0.md
autonomous: true
requirements: [STAGE-A]

must_haves:
  truths:
    - "A hook configured under `[[hooks.tool_execution_start]]` fires before a tool call; `tool_execution_end` / `input` / `session_shutdown` likewise replace their old keys on both the `[[hooks.*]]` surface and `NANOPI_EVENT`."
    - "A config containing any of the four retired keys is a hard config-load error naming the replacement — nanopi does not start. No alias, no dual-key parsing, no warn-and-accept path (§2.3)."
    - "A `session_shutdown` hook fires on `/new`, `/resume`, `/fork`, `/import`, and on quit, with `arguments.reason` = `new`/`resume`/`fork`/`import`/`quit`."
    - "A `session_start` hook fires on `/new`, `/resume`, `/fork`, `/import`, and at startup, with `arguments.reason` = `new`/`resume`/`fork`/`import`/`startup` (§2.2a)."
    - "For all four session-lifecycle events the payload's `session_id` is the REAL session id and the reason is in `arguments.reason` — the compaction events no longer put `threshold`/`manual` in `session_id` (§2.2b, full version)."
    - "The `matcher` subject is unchanged for every session event: session id for start/shutdown, compaction reason for the two compaction events."
    - "At each of the four agent-swap sites, `session_shutdown` fires against the OUTGOING agent and `session_start` against the INCOMING one, in that order — asserted by a unit test that checks the session ids, not by grep (§2.2c)."
    - "`cargo test --features wasm` is green at every commit."
    - "Both READMEs still claim the Claude Code hook *protocol* and no longer claim Claude Code event *names*, and carry the old→new rename table."
    - "`.release-notes-v0.12.0.md` announces the WHOLE v0.12.0 tag: it absorbs everything in `.release-notes-v0.11.0.md` (which will never ship on its own) plus the six fixes in `ead885a`, and its Compatibility section leads with this plan's three breaking changes."
  artifacts:
    - path: src/agent/hook.rs
      provides: "Renamed HookEvent variants, env_var strings, run_session_hooks with separate `arguments` / `subject` / `session_id` params, retired-key error mapping"
      contains: "ToolExecutionStart"
    - path: src/agent/loop_.rs
      provides: "fire_session_shutdown(reason) + fire_session_start(reason); all four session events pass a real session id and a reason"
      contains: "fire_session_shutdown"
    - path: src/mode/tui.rs
      provides: "swap_agent_with_reason seam + its ordering test + the four call sites"
      contains: "swap_agent_with_reason"
    - path: .release-notes-v0.12.0.md
      provides: "Release notes for the whole tag — extensions + fixes + this plan's breaks"
      contains: "tool_execution_start"
  key_links:
    - from: src/mode/tui.rs
      to: Agent::fire_session_shutdown
      via: "swap_agent_with_reason fires on the outgoing Agent before installing the new one"
      pattern: "swap_agent_with_reason\\("
    - from: src/mode/tui.rs
      to: Agent::fire_session_start
      via: "swap_agent_with_reason fires on the incoming Agent after installing it"
      pattern: "fire_session_start\\("
    - from: src/agent/loop_.rs
      to: run_session_hooks
      via: "reason passed as `arguments`, session id passed as its own param, subject unchanged"
      pattern: "json!\\(\\{\\s*\"reason\""
    - from: src/config.rs
      to: src/agent/hook.rs
      via: "toml unknown-field error mapped through the retired-key table"
      pattern: "retired_hook_key_error"
    - from: .release-notes-v0.12.0.md
      to: .release-notes-v0.11.0.md
      via: "v0.11.0's content absorbed — it ships inside v0.12.0, never on its own"
      pattern: "extension|WASM"
---

<objective>
Stage A of `docs/v0.12-events.md`: adopt PI's names for the four
Claude-Code-named hook events on both the `[[hooks.*]]` config surface and
the `NANOPI_EVENT` env var, make the retired keys a hard config error, and
give the session-lifecycle events an honest payload — `arguments.reason`
for every one of the four, a real session id in `session_id`, fired at all
five teardown sites and all five startup sites.

Purpose: one vocabulary for nanopi's eleven events, before Stage B wires
WASM plugins to the same event set. Ships alone; no WASM work here.
Output: renamed code + config surface, the `arguments.reason` convention,
a tested fire-before-swap seam, updated READMEs / `config.toml.example` /
`docs/v0.5-research.md` banner, and `.release-notes-v0.12.0.md` covering
the whole tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/v0.12-events.md
@.planning/STATE.md

@src/agent/hook.rs
@src/agent/loop_.rs
@src/settings.rs
@src/config.rs
@src/mode/tui.rs

@.release-notes-v0.11.0.md

**The spec is frozen and, as of `a5ca9bd`, internally consistent.** §2.1
(rename map), §2.2 (session `reason`, including review decisions (a), (b)
in its full form, and (c)), §2.3 (no compatibility period), and the Stage A
half of the §9 checklist are authoritative. Do not redesign; do not extend
into Stage B (no WASM, no `events` list, no `list-events`).

Branch is `v0.12.0`. Two untracked `.png` files at the repo root are not
yours — leave them alone. Do NOT `git checkout` / `git restore` any file,
and do NOT revert or amend `ead885a`, `36aed27`, `338b63c`, or `a5ca9bd`.

**Do NOT bump `VERSION` and do NOT create a tag.** Task 4 writes release
notes only; cutting the release is a separate call (and `release.yml`
hard-fails on a tag/VERSION mismatch, so guessing here is expensive).

<interfaces>
The rename map (spec §2.1 — the only four events that change):

| old key / variant / env | new key / variant / env |
|---|---|
| `pre_tool_use` · `PreToolUse` · `PreToolUse` | `tool_execution_start` · `ToolExecutionStart` · `ToolExecutionStart` |
| `post_tool_use` · `PostToolUse` · `PostToolUse` | `tool_execution_end` · `ToolExecutionEnd` · `ToolExecutionEnd` |
| `user_prompt_submit` · `UserPromptSubmit` · `UserPromptSubmit` | `input` · `Input` · `Input` |
| `session_end` · `SessionEnd` · `SessionEnd` | `session_shutdown` · `SessionShutdown` · `SessionShutdown` |

`HookEvent` carries `#[serde(rename_all = "snake_case")]`, so renaming the
variants renames the JSON payload's `event` value. The `[[hooks.*]]` table
keys are `HooksSection`'s **field names** (`src/settings.rs:49`) and are
renamed separately from the enum.

Field-name sites (all `Vec<HookConfig>`), verified at HEAD:
- `src/settings.rs:51,53,55,59` — `HooksSection` declarations
- `src/settings.rs:79-83,103-109,131-135,150-154,170-175` — `From<Settings>`, three merge blocks, `validate_hooks` calls
- `src/config.rs:272-289` — `merge()`
- `src/agent/loop_.rs:55-59` — `HooksConfig` declarations
- `src/mode/tui.rs:3320-3324` — hook-count display

Session hook plumbing, as it stands at HEAD:
- `run_session_hooks(hooks, event, subject, cwd)` — `src/agent/hook.rs:396`; hardcodes `arguments: json!({})` at 423 and `session_id: Some(subject.to_string())` at 425; `debug_assert!` at 405 lists accepted variants; doc comment at 386–394 documents the `subject` overload.
- Its 4 callers: `src/agent/loop_.rs:286` (session_start, subject = session id), `302` (session_end, subject = session id), `330` + `365` (both inside `compact_now`, subject = `"threshold"`/`"manual"` — this is the (b) defect). Both compaction sites are on `&mut self`, so `self.session_id` is in scope for the new parameter.

Target signature after Task 2:

```
run_session_hooks(
    hooks: &[HookConfig],
    event: HookEvent,
    arguments: Value,     // {"reason": …} for all four session events
    subject: &str,        // what `matcher` is tested against — UNCHANGED
    session_id: &str,     // the real session id — always
    cwd: &Path,
)
```

**Emit-site census (measured — bigger than the spec's table implies).**
`fire_session_end` has 2 callers: `src/mode/tui.rs:505`, `src/mode/print.rs:188`.
`fire_session_start` has 2 callers: `src/mode/tui.rs:431`, `src/mode/print.rs:178`.
Neither fires at any of the four agent-swap sites today. So decision (a) is
not "add a reason to an existing call" — the `new`/`resume`/`fork`/`import`
reasons for `session_start` have **no emit site at all** right now, and
four must be created alongside the four `session_shutdown` ones. Both
halves land in the same helper (decision (c)), which is why they are one
task.

Agent-swap sites in `src/mode/tui.rs` — all four are the byte-identical
block `{ let mut g = agent_slot.lock().await; *g = Some(new_agent); }`,
which is exactly what the (c) helper replaces:
- `1907-1910` — `KeyAction::NewSession` → reason `new`
- `2024-2027` — `KeyAction::OpenResumePicker` → reason `resume`
- `2474-2477` — `KeyAction::ApplyImport` → reason `import`
- `3160-3163` — `execute_fork` → reason `fork`

`Agent`'s fields are all `pub` (`src/agent/loop_.rs:74-140`) and
`src/mode/tui.rs` already has a `#[cfg(test)] mod tests` at 4681, so the
(c) helper is unit-testable there by building two Agents with a struct
literal — the pattern `src/agent/loop_.rs`'s tests use (see `tmp()` and
`fake_provider()` at 1568–1584).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename the four events across code, doc comments, and tests</name>
  <files>src/agent/hook.rs, src/agent/loop_.rs, src/agent/permission.rs, src/config.rs, src/settings.rs, src/settings_toml.rs, src/mode/tui.rs</files>
  <behavior>
    - `HookEvent::ToolExecutionStart.env_var() == "ToolExecutionStart"`; same for `ToolExecutionEnd`, `Input`, `SessionShutdown`.
    - `serde_json::to_string(&HookEvent::Input) == "\"input\""` and round-trips; same for the other three snake_case forms.
    - A `HookInput { event: HookEvent::Input, .. }` serializes with `"event":"input"` (replaces the existing `"event":"user_prompt_submit"` assertion at `src/agent/hook.rs:867`).
    - `[[hooks.tool_execution_start]]` in config.toml loads into `HooksSection::tool_execution_start`; the config.rs merge test still concatenates two sources in order.
    - `PermissionMode::should_honor_tool_execution_start_block()` keeps its existing truth table.
  </behavior>
  <action>
Do this rename **by hand, per site — not with sed.** A blind sed was tried
and reverted; these are the traps it hit and they are the reason this task
exists as its own commit. Per §2.3 this is a pure rename — no aliases and
no old-name fallbacks anywhere.

1. `src/agent/hook.rs`: rename the four `HookEvent` variants and their
   `env_var()` arms per the table in `<interfaces>`. Update the module doc
   comment (lines 1–20) so its `[[hooks.pre_tool_use]]` example and its
   snake_case key list use the new names. Update the `run_hooks` doc
   comment at 318–322 (`post_tool_use` / `pre_tool_use` prose) and the
   `run_session_hooks` doc comment at 386–394 plus its `debug_assert!`
   (`HookEvent::SessionEnd` → `SessionShutdown`).
2. `src/settings.rs`: rename the four `HooksSection` fields and every
   downstream use (`From<Settings>`, the three merge blocks, the
   `validate_hooks` calls) per the site list in `<interfaces>`. Rename the
   tests and their embedded TOML: `loads_pre_tool_use` →
   `loads_tool_execution_start`, and the `[[hooks.pre_tool_use]]` /
   `[[hooks.post_tool_use]]` fixture text at 253, 259, 292, 304, 391 plus
   the assertions at 240–241, 269–271, 328–330 and the comments at 288/300.
3. `src/config.rs`: rename the `merge()` field moves and extends
   (272–289), the fixture TOML at 493, the assertions at 508–509, and the
   merge test's `HooksSection` literals at 517–543.
4. `src/agent/loop_.rs`: rename the four `HooksConfig` fields (55–59) and
   every use. **`user_prompt_submit` → `input` is the dangerous one**: the
   file is full of the unrelated word `input` (`HookInput`, `tool_input`,
   `updated_input`, `let input = HookInput { .. }`, `input_json`). Only the
   `self.hooks.user_prompt_submit` / `hooks.user_prompt_submit` /
   `user_prompt_submit:` struct-literal occurrences (523, 525, 1974) and
   the `HookEvent::UserPromptSubmit` at 526 change. Also update the prose
   and user-visible strings: the comments at 460, 500, 516, 519, 567, 1408,
   1489–1491, 1522, 1756–1757, 1763, 1870, 1941, 2716, 2740; the blocked
   marker string at 539 (`[UserPromptSubmit hook blocked the prompt: ...]`
   → `[Input hook blocked the prompt: ...]`); the test names
   `post_tool_use_hook_receives_input_and_response` →
   `tool_execution_end_hook_receives_input_and_response`,
   `post_tool_use_hook_can_transform_result` →
   `tool_execution_end_hook_can_transform_result`,
   `before_agent_start_output_feeds_user_prompt_submit` →
   `..._feeds_input`; the payload assertion at 1740
   (`v["event"] == "post_tool_use"` → `"tool_execution_end"`); and the
   assertion messages at 2028 and 2781.
5. `src/agent/permission.rs`: `should_honor_pretooluse_block` →
   `should_honor_tool_execution_start_block` (60), its doc comment (57),
   the test name `pretooluse_block_always_honored` →
   `tool_execution_start_block_always_honored` (143) and its call (145).
   This identifier contains no `pre_tool_use` substring, which is exactly
   why the sed attempt missed it. Update the caller at
   `src/agent/loop_.rs:1423`.
6. `src/mode/tui.rs:3320-3324`: rename the four fields in the hook-count
   sum.
7. `src/settings_toml.rs:245,264`: the test fixture writes
   `[[hooks.pre_tool_use]]` and asserts it survives a `save()`. Rename both
   to `tool_execution_start` — the point of the test is comment/unknown-
   section preservation, and it must not depend on a retired key.

Leave `fire_session_end` / `fire_session_start` and their callers alone in
this task except for the `HookEvent::SessionShutdown` /
`self.hooks.session_shutdown` bodies — the signatures, `reason`, and new
emit sites are Tasks 2 and 3.

After editing, prove no stragglers remain (see `<verify>`); the counts in
the plan brief were taken at HEAD and a nonzero residue means a missed site.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; ! grep -rniE 'pre_tool_use|PreToolUse|post_tool_use|PostToolUse|user_prompt_submit|UserPromptSubmit|pretooluse' src/ &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>`cargo test --features wasm` green (565 unit + 6 + 22 integration, no
  net change); clippy clean; zero occurrences of the three old
  tool/prompt names anywhere under `src/`. Committed as one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest session payloads — separate subject / session_id, reason in arguments (§2.2 + a + b)</name>
  <files>src/agent/hook.rs, src/agent/loop_.rs, src/mode/print.rs, src/mode/tui.rs</files>
  <behavior>
    - `session_shutdown` payload: `arguments == {"reason":"quit"}` (or `new`/`resume`/`fork`/`import`), `session_id == <the agent's session id>`.
    - `session_start` payload: `arguments == {"reason":"startup"}` (or `new`/`resume`/`fork`/`import`), `session_id == <the agent's session id>`.
    - `session_before_compact` / `session_compact` payload: `arguments == {"reason":"threshold"}` (or `"manual"`) AND `session_id == <the agent's real session id>` — **not** the reason. This is the (b) fix: that field used to hold `"threshold"`.
    - `session_before_compact` with `matcher = "^threshold$"` still fires for reason `threshold` and not for `manual` — the matcher subject stays the reason even though the payload's `session_id` no longer does.
    - `session_shutdown` with `matcher = "^prod-"` still does not fire for session id `dev-1234` — the matcher subject for start/shutdown stays the session id.
    - `--no-hooks` suppresses both `session_start` and `session_shutdown`.
  </behavior>
  <action>
Per spec §2.2 and review decisions (a) and (b) — (b) in its **full** form:
the subject and the session id become separate parameters, so no payload
field lies afterwards. The narrow version (add `arguments.reason`, leave
`session_id` alone) is explicitly rejected by the spec — it would leave the
lying field in place and need a second breaking payload change later.

1. `src/agent/hook.rs` — change `run_session_hooks` to the signature in
   `<interfaces>`: insert `arguments: Value` after `event`, and
   `session_id: &str` after `subject`. Use `arguments` at line 423 in
   place of `json!({})`, and `session_id` at 425 in place of
   `subject.to_string()`. The `NANOPI_SESSION_ID` env var (429) must also
   carry the real session id now, not the subject — a `session_compact`
   hook currently gets `NANOPI_SESSION_ID=threshold`, which is the same
   bug in the env surface. Rewrite the doc comment (386–394) around the
   new three-way split, stated as a table:
   `session_id` = the real id, always · `arguments.reason` = the reason,
   always · `subject` = what `matcher` tests against, still deliberately
   overloaded (session id for start/shutdown, reason for compaction).
   Note that a hook reading `session_id` to get `threshold`/`manual` must
   now read `arguments.reason`.
2. `src/agent/loop_.rs`:
   - `fire_session_start` → `fire_session_start(&self, reason: &str)`,
     passing `json!({"reason": reason})`, subject and session_id both
     `&self.session_id.to_string()`. Keeps the `hooks_active()` gate.
   - `fire_session_end` → `fire_session_shutdown(&self, reason: &str)`,
     same shape, `HookEvent::SessionShutdown`.
   - Both doc comments list their reason vocabularies —
     `startup|new|resume|fork|import` and `quit|new|resume|fork|import` —
     and record the two deliberate divergences from PI: no `reload`
     (nanopi's `/reload` updates the live Agent in place via
     `refresh_live_agent`, so no session ends or starts) and `import`
     added (nanopi has `/import`, PI does not).
   - The two `compact_now` call sites (330, 365) pass
     `json!({"reason": reason})` as `arguments`, keep `reason` as
     `subject`, and pass `&self.session_id` as the new `session_id`. Do
     NOT change the subject — the matcher contract for these two events is
     the reason and that is documented, shipped behavior. Update the two
     comments above them (323–326, 360–361) to say the matcher still tests
     the reason while the payload now carries both fields honestly.
3. `src/mode/print.rs`: `fire_session_start("startup")` (178) and
   `fire_session_shutdown("quit")` (188).
4. `src/mode/tui.rs`: one-line each at 431 (`"startup"`) and 505
   (`"quit"`), so this task compiles standalone. The four swap sites are
   Task 3 — leave them.
5. Tests in `src/agent/loop_.rs`'s test module. Use a hook command of
   `cat > <tmpfile>` so the whole stdin payload is captured and parsed as
   JSON (the temp-file-marker pattern from
   `run_session_hooks_fires_matching_hook` in `hook.rs`):
   - `session_shutdown` payload carries `"reason":"new"`,
     `"event":"session_shutdown"`, and `session_id` == the agent's id.
   - `session_start` payload carries `"reason":"startup"` and
     `"event":"session_start"`.
   - **(b) regression test**, named for the old behavior: a
     `session_before_compact` hook's payload has
     `arguments.reason == "threshold"` AND `session_id == <the agent's
     real session id>`. Comment that `session_id` used to hold
     `"threshold"` — a field whose name was a lie — and that this test is
     what keeps it honest.
   - keep both existing matcher tests (`^prod-` for shutdown in `hook.rs`,
     `^threshold$` for compaction) working with the new signature; they
     are the guard against "fixing" the subject as well as the payload.
   - extend the existing `--no-hooks` lifecycle test (~2716) to the new
     signatures.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; ! grep -rn 'fire_session_end\|HookEvent::SessionEnd' src/ &amp;&amp; ! grep -n 'json!({})' src/agent/hook.rs &amp;&amp; ! grep -n 'session_id: Some(subject' src/agent/hook.rs &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>`run_session_hooks` takes `arguments`, `subject`, and `session_id`
  separately; no `json!({})` and no `session_id: Some(subject…)` left in
  `hook.rs`; no `fire_session_end` left; the three payload tests and the
  (b) regression test green, both matcher tests still green; suite green;
  clippy clean. Committed as one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The fire-before-swap seam and the four agent-swap emit sites (§2.2c)</name>
  <files>src/mode/tui.rs</files>
  <behavior>
    - Calling the seam with an outgoing agent whose session id is `A` and an incoming agent whose id is `B`, with reason `new`, produces exactly two hook firings in this order: `session_shutdown` with `arguments.reason == "new"` and `session_id == A`, then `session_start` with `arguments.reason == "new"` and `session_id == B`.
    - After the call, `agent_slot` holds the incoming agent.
    - The shutdown firing must observe `A`. A one-line-late implementation (swap first, then fire) makes the test fail because the shutdown payload would carry `B`.
    - With `--no-hooks` (a `PermissionGate` where `hooks_active()` is false) the seam swaps the agent and fires nothing.
  </behavior>
  <action>
Decision (c). This is the minimum seam that makes the ordering assertable —
**not** a refactor of the four sites' surrounding logic. Everything else at
those sites (building `new_agent`, `print_skill_diagnostics`,
`set_active_session`, the `app.*` resets) stays exactly where it is.

1. Add one free async fn to `src/mode/tui.rs`:

   `async fn swap_agent_with_reason(agent_slot: &Arc<Mutex<Option<Agent>>>, new_agent: Agent, reason: &str)`

   Body, in this order and documented as order-critical: take the guard;
   if the slot holds an agent, `await` `fire_session_shutdown(reason)` on
   it; `*g = Some(new_agent)`; `await` `fire_session_start(reason)` on the
   now-installed agent. Doc comment states why the order is load-bearing —
   one line late and `session_shutdown` reports the incoming session, so an
   audit or cleanup hook attributes the teardown to the wrong session id.
   Holding the mutex across both awaits is correct: same task, and the
   hook subprocess does not re-enter `agent_slot`.

2. Replace the four identical
   `{ let mut g = agent_slot.lock().await; *g = Some(new_agent); }` blocks
   with a `swap_agent_with_reason(agent_slot, new_agent, "<reason>").await`
   call: `new` (1907–1910), `resume` (2024–2027), `import` (2474–2477),
   `fork` (3160–3163). `execute_fork`'s site is in a free fn that already
   has `agent_slot: &Arc<Mutex<Option<Agent>>>` in scope, and the other
   three are inside the key-action handler with the same binding — no
   plumbing needed. Leave `src/mode/tui.rs:431`
   (`fire_session_start("startup")`) and `505`
   (`fire_session_shutdown("quit")`) as direct calls; they are not swaps.

3. Ordering test in `src/mode/tui.rs`'s existing `#[cfg(test)] mod tests`
   (4681), as a `#[tokio::test]`. `Agent`'s fields are all `pub` and
   `OpenAiProvider::new("http://localhost:0", "fake-key", "fake-model")`
   never makes a call, so build two Agents by struct literal — mirror
   `src/agent/loop_.rs`'s test helpers (`tmp()`, `fake_provider()` at
   1568–1584); duplicate them locally rather than exporting them.
   Give both agents a `HooksConfig` with the SAME hook wired to both
   `session_start` and `session_shutdown`: command `cat >> <tmpfile>` —
   appending, so the file becomes an ordered transcript of both firings.
   Then assert on the transcript:
   - two JSON objects, in order;
   - first is `session_shutdown` / `reason: "new"` / `session_id: A`;
   - second is `session_start` / `reason: "new"` / `session_id: B`;
   - `agent_slot` now holds session id `B`.
   Comment that asserting `session_id: A` on the first object is the
   actual ordering assertion — the event names alone would still pass if
   the swap happened first.
   Add the `--no-hooks` variant: same setup with a `PermissionGate` that
   disables hooks, assert the transcript file does not exist and the slot
   still got swapped.

Keep the grep gate as a secondary check (see `<verify>`) — it catches a
missed site cheaply, while the test above is what proves the ordering.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; test "$(grep -c 'swap_agent_with_reason(agent_slot' src/mode/tui.rs)" = 4 &amp;&amp; test "$(grep -rho 'swap_agent_with_reason(agent_slot, new_agent, "[a-z]*")' src/mode/tui.rs | sort -u | wc -l)" = 4 &amp;&amp; ! grep -n '\*g = Some(new_agent)' src/mode/tui.rs &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>Four call sites, four distinct reasons, zero remaining raw
  `*g = Some(new_agent)` swap blocks; the ordering test fails if the fire
  and the swap are transposed (verify this once by hand-transposing the
  helper body, watching it fail, then restoring); the `--no-hooks` variant
  green; suite green; clippy clean. Committed as one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Retired keys are a hard error; docs, config example, whole-tag release notes</name>
  <files>src/settings.rs, src/config.rs, src/agent/hook.rs, config.toml.example, README.md, README_zh.md, docs/v0.5-research.md, .release-notes-v0.12.0.md</files>
  <behavior>
    - Loading a `config.toml` containing `[[hooks.pre_tool_use]]` returns `Err` (hard error — nanopi does not start), and the message names both the retired key and `tool_execution_start` and points at `docs/v0.12-events.md §2.1`.
    - Same for `post_tool_use` → `tool_execution_end`, `user_prompt_submit` → `input`, `session_end` → `session_shutdown`, in `settings.toml` as well as `config.toml`.
    - A hook key that is merely misspelled (e.g. `[[hooks.turn_startt]]`) is also a hard error, falling through to serde's unknown-field message listing the valid keys.
    - A `config.toml` using only current keys still loads clean (the existing loader tests).
    - `.release-notes-v0.12.0.md` exists, follows the house style of `.release-notes-v0.11.0.md`, covers the extension feature set AND the `ead885a` fixes AND this plan's renames, and its `## Compatibility` section opens with the four-row rename table.
  </behavior>
  <action>
Spec §2.3's "the one config typo that must be loud", load-bearing because
§2.3 removed the compatibility path entirely.

1. Add `#[serde(deny_unknown_fields)]` alongside the existing
   `#[serde(default)]` on `HooksSection` (`src/settings.rs:47-49`). Both
   `config.toml` (via `Config`) and `settings.toml` (via `Settings`) reach
   the same struct, so one attribute covers both surfaces. Comment that
   this is what makes a retired key a parse error instead of a silent
   no-op, and that per §2.3 there is deliberately no alias to soften it.
2. Add to `src/agent/hook.rs`:
   `pub fn retired_hook_key_error(err: &str) -> Option<String>` — a
   four-entry table (`pre_tool_use` → `tool_execution_start`,
   `post_tool_use` → `tool_execution_end`, `user_prompt_submit` → `input`,
   `session_end` → `session_shutdown`) scanned against a rendered toml
   error's text, returning the §2.3 message verbatim in shape:
   `unknown hook event "pre_tool_use" — renamed to "tool_execution_start" in v0.12 (see docs/v0.12-events.md §2.1)`
   Unit-test all four mappings plus a `None` case (an unrelated error
   string must not be rewritten).
3. Wire it into both error paths so the mapped message reaches the user in
   place of serde's bare "unknown field": `src/config.rs::load_one` (258)
   and `src/settings.rs::load_one`. When `retired_hook_key_error` yields a
   message, surface that (keeping the file path so the user knows which of
   the four config files to edit); otherwise keep today's error verbatim.
   Add a loader test in each file asserting the `Err` string contains both
   `pre_tool_use` and `tool_execution_start`, plus one asserting a
   misspelled-but-never-shipped key (`turn_startt`) is still an `Err`.
4. `config.toml.example` (195–245): rename the four commented keys; add a
   "renamed in v0.12 — the old keys are errors, not warnings" block with
   the four-row table above the hooks section; and document the session
   payload contract — `arguments.reason` for all four session events,
   `session_id` always the real id, and that compaction hooks must now
   read `arguments.reason` rather than `session_id`.
5. `README.md` — line 45 (feature bullet), 195 (section intro), 198 (TOML
   sample), 203 (snake_case note), 207 (lifecycle paragraph). Keep the
   Claude Code **protocol** claim (JSON on stdin, exit 2 to refuse,
   `tool_name` / `tool_input` / `hookSpecificOutput` fields) and drop the
   Claude Code **naming** claim: the event names are PI's now. Add the
   four-row rename table, and say plainly that the old keys are rejected at
   startup. Leave line 122 (`-p` mode semantics) and 347 (credits) alone —
   those claims are still true. Mirror all of it in `README_zh.md`
   (44, 190, 193, 198, 202); per the repo's bilingual convention the
   Chinese version is a real translation, not a stub.
6. `docs/v0.5-research.md` §6 (line 647): add a one-line banner
   immediately under the heading pointing at `docs/v0.12-events.md` §2.1
   for the current event names. **Do not rewrite the section** — it is a
   historical record of the v0.5 design.
7. `.release-notes-v0.12.0.md` — this announces the **whole tag**, not
   just the event work. v0.11.0 is sealed but will never be released on
   its own; it ships inside v0.12.0. So absorb
   `.release-notes-v0.11.0.md` rather than writing a rename changelog.
   Read two inputs first: `.release-notes-v0.11.0.md` in full (house
   style: `# nanopi vX.Y.Z`, 2–3 sentence lede, `## Highlights` with
   `###` subsections, `## Compatibility`, `## Under the hood`), and
   `git log -1 ead885a` (a detailed six-fix commit message — mine it, do
   not re-derive the fixes from the diff).

   `## Highlights` — carry over v0.11.0's subsections, updating names to
   post-rename where they appear: mid-stream steering; WASM extensions
   (including the gated `host-fs-read` / `host-http-get`); **plugin slash
   commands** (shipped after v0.11.0's notes were written — see STATE.md's
   2026-09-02 entry, so it is not in the v0.11 file and must be added);
   the Pi lifecycle hooks; `tool_execution_end` can rewrite results;
   configurable tool exec mode; `write`/`edit` confined to the working
   directory. Then a new subsection for this plan: one vocabulary for the
   eleven events.
   Add a `### Fixes` subsection from `ead885a`: `/tools` (the model could
   not reliably say what it can call), parallel tool-card headers keyed by
   call_id, `url_allowlist` wildcard patterns, the blockquote/thinking
   style collision, the musl libunwind `bad fde` noise, and the `-p`
   thinking/reply run-on.

   `## Compatibility` **leads with the breaking changes** — unlike
   v0.11.0's "No breaking changes", which is now wrong for this tag —
   three of them, in this order:
   1. the four-row rename table, then the statement that the old keys are
      a startup error naming the replacement, with no deprecation period
      (§2.3, and why: effectively no users of these keys yet);
   2. `session_shutdown` and `session_start` now fire on `/new`,
      `/resume`, `/fork`, `/import` as well as at quit/startup, and both
      carry `arguments.reason`. A hook that assumed "fires once, at exit"
      must branch on `reason`. `matcher` subject unchanged;
   3. the session payload is now honest: `session_id` is always the real
      session id and the reason is always in `arguments.reason`. A
      compaction hook reading `session_id` to get `threshold`/`manual`
      must read `arguments.reason` — and `NANOPI_SESSION_ID` likewise
      stops carrying the reason. `matcher` for those two events still
      tests the reason, so matchers do not change.
   Keep v0.11.0's own compatibility note that `write`/`edit` tightened.

   `## Under the hood`: v0.11.0's entries plus `NANOPI_EVENT` values
   follow the rename, `HooksSection` gained `deny_unknown_fields`, and
   `run_session_hooks` gained `arguments` + a separate `session_id`.

   Do not describe Stage B (WASM event delivery) — it has not shipped.
   Do not touch `VERSION` and do not tag.
  </action>
  <verify>
    <automated>cargo test --features wasm 2>&amp;1 | tail -30 &amp;&amp; grep -q 'deny_unknown_fields' src/settings.rs &amp;&amp; grep -c 'tool_execution_start' README.md README_zh.md config.toml.example .release-notes-v0.12.0.md &amp;&amp; grep -q 'arguments.reason' .release-notes-v0.12.0.md config.toml.example &amp;&amp; grep -qi 'steer' .release-notes-v0.12.0.md &amp;&amp; grep -qi 'slash command' .release-notes-v0.12.0.md &amp;&amp; grep -q 'url_allowlist' .release-notes-v0.12.0.md &amp;&amp; grep -q '/tools' .release-notes-v0.12.0.md &amp;&amp; grep -A2 '^## 6' docs/v0.5-research.md | grep -q 'v0.12-events' &amp;&amp; grep -q '^## Compatibility' .release-notes-v0.12.0.md &amp;&amp; ! grep -rn 'serde(alias' src/settings.rs src/config.rs &amp;&amp; test -z "$(git status --porcelain VERSION)" &amp;&amp; cargo clippy --features wasm --all-targets -- -D warnings</automated>
  </verify>
  <done>A `[[hooks.pre_tool_use]]` config fails to load with an error naming
  both the retired key and `tool_execution_start`, proven by a test in each
  of `config.rs` and `settings.rs`; no `serde(alias …)` anywhere; both
  READMEs and `config.toml.example` carry the rename table;
  `.release-notes-v0.12.0.md` covers extensions + plugin slash commands +
  the six `ead885a` fixes + all three breaking changes; `VERSION`
  untouched and no tag created; `docs/v0.5-research.md` §6 has the banner
  and is otherwise unchanged (`git diff --stat` shows a 1–2 line delta for
  that file); suite green; clippy clean. Committed as one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config.toml` / `settings.toml` → hook runtime | user-authored TOML selects shell commands nanopi will execute |
| hook subprocess stdin/stdout → agent loop | hook output can block or rewrite tool arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m0z-01 | Tampering | retired `[[hooks.pre_tool_use]]` key | mitigate | Task 4: `deny_unknown_fields` + the four-entry retired-key table, surfaced as a hard load error. With §2.3 removing the alias path, a blocking guard (`check-rm-rf.sh`) silently un-registering after upgrade is the failure mode this error exists to prevent — which is why it must abort startup, not warn. |
| T-m0z-02 | Repudiation | `session_shutdown` at agent-swap sites | mitigate | Task 3: `swap_agent_with_reason` fires shutdown on the outgoing agent before installing the new one, and the ordering test asserts `session_id == A` on the shutdown payload. Without that assertion an audit hook silently attributes every `/new` teardown to the incoming session. |
| T-m0z-03 | Repudiation | compaction events' `session_id` / `NANOPI_SESSION_ID` | mitigate | Task 2 (§2.2b full): both used to carry `threshold`/`manual`, so an audit hook logging compactions recorded no session at all — every entry attributed to a session named "threshold". The (b) regression test asserts the real id. |
| T-m0z-04 | Information disclosure | `reason` in the payload | accept | The reason vocabularies are fixed literals chosen by nanopi (`quit\|new\|resume\|fork\|import`, `startup\|new\|resume\|fork\|import`, `threshold\|manual`); no user data, no path, no prompt text crosses the boundary. |
| T-m0z-05 | Elevation of privilege | `matcher` subject semantics | accept | Spec §2.2 keeps the subject as-is for all four events. Decision (b) changes the payload and env var only, so no existing matcher's effective scope changes. Task 2's two retained matcher tests are the guard. |
| T-m0z-06 | Denial of service | session events now fire at four more sites, ×2 | accept | `/new`, `/resume`, `/fork`, `/import` now each run up to two hooks that previously did not fire there, bounded by each hook's own `timeout` (default 5000 ms) — worst case ~10 s added to an interactive command. Same bound already accepted at quit. Called out in the release notes' Compatibility section so it is a known cost. |
| T-m0z-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan (`Cargo.toml` is untouched). If a task turns out to need one, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
1. `cargo test --features wasm` green after each of the four commits —
   565 unit + 6 + 22 integration at HEAD, and this plan adds tests without
   removing any, so the counts only go up.
2. `cargo clippy --features wasm --all-targets -- -D warnings` clean.
3. `grep -rniE 'pre_tool_use|PreToolUse|post_tool_use|PostToolUse|user_prompt_submit|UserPromptSubmit|pretooluse|fire_session_end|HookEvent::SessionEnd' src/`
   → only the retired-key table in `src/agent/hook.rs` and its tests, and
   the loader tests that assert the error. Nothing in a live code path.
   (`docs/v0.5-research.md`, `docs/PLAN.md`, `docs/pi-vs-nanopi.md`, and
   `.planning/` keep the old names on purpose — historical records. Only
   `src/`, `config.toml.example`, the two READMEs, and the release notes
   are in scope.)
4. `git status --porcelain` shows only the two untracked root `.png` files
   beyond the committed changes. `git diff HEAD~4 --name-only` does not
   include `VERSION`, and `git tag --points-at HEAD` is empty.
5. Manual smoke, all four session events at once:
   `printf '[[hooks.session_shutdown]]\nmatcher="*"\ncommand="cat >>/tmp/npsess.jsonl"\n\n[[hooks.session_start]]\nmatcher="*"\ncommand="cat >>/tmp/npsess.jsonl"\n\n[[hooks.session_compact]]\nmatcher="*"\ncommand="cat >>/tmp/npsess.jsonl"\n' > .nanopi/config.toml`,
   run `nanopi -p hi`, then confirm `/tmp/npsess.jsonl` holds a
   `session_start` with `"reason":"startup"` followed by a
   `session_shutdown` with `"reason":"quit"`, both with a real UUID in
   `session_id`.
   Then in the TUI run `/new` and confirm two more lines appear: a
   `session_shutdown` with `"reason":"new"` carrying the OLD session id,
   followed by a `session_start` with `"reason":"new"` carrying the new
   one. Run `/compact` and confirm the `session_compact` line has
   `arguments.reason == "manual"` and a real UUID in `session_id`.
   Then swap a key to `[[hooks.pre_tool_use]]` and confirm nanopi
   **refuses to start**, printing a message that names
   `tool_execution_start`.
</verification>

<success_criteria>
- Every Stage A box in `docs/v0.12-events.md` §9 is satisfied: enum +
  `env_var()`, `HooksSection` keys in all three files, hard unknown-key
  error, `fire_session_shutdown(reason)` at all five sites, `permission.rs`
  rename, README ×2 + `config.toml.example` + rename table,
  `docs/v0.5-research.md` §6 banner.
- All three review decisions honored: (a) `session_start` carries the same
  `reason` and fires at the four swap sites; (b) in full — `subject` and
  `session_id` are separate parameters, so `session_id` and
  `NANOPI_SESSION_ID` always carry the real id and the reason always lives
  in `arguments.reason`; (c) the fire-before-swap ordering is asserted by a
  unit test that checks session ids, not by grep.
- §2.3 honored: zero aliases, zero dual-key parsing, zero warn-and-accept
  paths for the four retired keys.
- `.release-notes-v0.12.0.md` covers the whole tag — v0.11.0's absorbed
  content, plugin slash commands, the six `ead885a` fixes, and this plan's
  three breaking changes leading Compatibility.
- `VERSION` untouched, no tag created.
- Four commits, suite green at each.
- No Stage B work leaked in (no `.wit` change, no `events` config field,
  no `list-events` / `handle-event`).
</success_criteria>

<output>
Create `.planning/quick/260902-m0z-rename-hook-events-pi-names/260902-m0z-SUMMARY.md` when done
</output>
